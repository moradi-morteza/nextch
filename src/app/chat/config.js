export const MAX_UPLOAD_MB = 5;
export const MAX_UPLOAD_BYTES = MAX_UPLOAD_MB * 1024 * 1024;

// Message media display sizing (not full-screen)
export const IMAGE_MAX_WIDTH_PX = 280;   // max bubble image width
export const IMAGE_MAX_HEIGHT_PX = 380;  // cap height so it won't fill screen
